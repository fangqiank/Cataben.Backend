// Infrastructure implements the Application-layer abstractions, so every file
// here needs to see the Repositories/Services namespaces. Declared globally to
// avoid editing each repository/service implementation individually.
global using Cataben.Application.Repositories;
global using Cataben.Application.Services;
